package com.example.SWPP.repository;

import com.example.SWPP.entity.SurveyType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SurveyTypeRepository extends JpaRepository<SurveyType, Integer> {
}
