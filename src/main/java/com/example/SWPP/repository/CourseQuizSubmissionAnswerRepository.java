package com.example.SWPP.repository;

import com.example.SWPP.entity.CourseQuizSubmissionAnswer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CourseQuizSubmissionAnswerRepository extends JpaRepository<CourseQuizSubmissionAnswer, Long> {
}
